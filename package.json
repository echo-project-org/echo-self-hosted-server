{
  "name": "echo-self-hosted-server",
  "version": "1.0.0",
  "description": "Self-hosted server for Echo",
  "license": "ISC",
  "author": "Kury & Thundy",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "deno --allow-env --allow-sys --allow-read --allow-write --allow-net index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "sqlite3": "^5.1.7",
    "type": "^2.7.3",
    "typescript": "^5.8.3",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.10"
  }
}
